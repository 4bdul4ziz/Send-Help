
/*Write a C program to accept a string on length of N. The program should print the input in a way that it should print the characters in a string number of times based on its position.

Example:

input:

God

output:

Gooddd*/


#include<stdio.h>

char str(100);
int count = 0, position, length;
int main(){
    scanf("%s", &str);
    for(strlen;)
    


    return 0;
}